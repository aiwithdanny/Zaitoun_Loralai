UI: Redesign reviews section to match reference layout

Changes:
- ReviewSummary: redesigned as vertical stacked left-column panel
  (big rating star + count + distribution bars), replaces old
  horizontal flex layout
- ReviewList: redesigned cards with name + Verified badge + date on
  same row, stars below, review text below that
- ProductDetail: reviews section now uses two-column grid
  (280px summary left, reviews+form right) on desktop, single
  column stacked on mobile

Co-authored-by: CommandCodeBot <noreply@commandcode.ai>
