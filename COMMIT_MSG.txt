Debug: expose real backend error when DEBUG=true for order 500 issue

The orders endpoint returns a generic 500 with no detail. Added
DEBUG-aware exception handling: when DEBUG=true, the actual Python
exception message is returned in the response body so it's visible
in the browser console. In production (DEBUG not set), it continues
to return the generic message.

Instructions: Set DEBUG=true on the production env, redeploy, attempt
an order, and the browser console will show the actual error (e.g.
missing table, column, FK constraint, etc.). Then fix and revert
DEBUG to false.

Co-authored-by: CommandCodeBot <noreply@commandcode.ai>
