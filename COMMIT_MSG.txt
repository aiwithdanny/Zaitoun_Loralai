Fix: review upload-image route ordering + CORS config

- Moved @router.post("/upload-image") above @router.post("/{product_group_id}")
  to fix 422 error — the parameterized route was swallowing literal paths
- Added CORS_ORIGINS and FRONTEND_URL env var support for explicit origins
  alongside the wildcard fallback, so Vercel production/preview URLs are
  explicitly allowed

Co-authored-by: CommandCodeBot <noreply@commandcode.ai>
