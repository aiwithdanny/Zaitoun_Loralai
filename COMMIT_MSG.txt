Phase A.2: Admin Review Moderation UI

New admin page at /admin/reviews with:
- Filter tabs: All / Pending (with count badge) / Approved / Rejected
- Review cards showing: reviewer name, star rating, review text,
  product group name, date, photo thumbnail (clickable), verified buyer badge,
  status badge (Pending/Approved/Rejected)
- Approve/Reject actions on pending reviews (soft-delete via rejecting)
- Pagination (10 per page, Prev/Next + page numbers)
- Loading, error, and empty states

Sidebar: "Reviews" link added between Products and Orders
Route: ProtectedRoute(AdminReviews, AdminLayout) at /admin/reviews

Co-authored-by: CommandCodeBot <noreply@commandcode.ai>
