Revert: restore original orders routes (redirect_slashes, trailing slash)

The 307 redirect was never the problem — it already had CORS headers
from the ASGI wrapper. The only real CORS fix needed was the
_cors_response() on exception handlers, which is already deployed.

Reverted:
- redirect_slashes=False → default True (removed from FastAPI constructor)
- @router.post("") → @router.post("/")
- @router.get("") → @router.get("/")

Co-authored-by: CommandCodeBot <noreply@commandcode.ai>
