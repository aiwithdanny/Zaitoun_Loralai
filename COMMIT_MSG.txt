Fix: alembic env.py now loads DATABASE_URL from .env file

Alembic was failing with "DATABASE_URL environment variable is required"
because env.py read from os.getenv() only — it never loaded the .env file
that already exists in the backend/ directory.

Added load_dotenv() pointing to backend/.env so DATABASE_URL is available
to Alembic without manually setting the env var in the shell.

Co-authored-by: CommandCodeBot <noreply@commandcode.ai>
