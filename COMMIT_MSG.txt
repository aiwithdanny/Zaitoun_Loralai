Fix: approved reviews filter + add delete functionality

Bug fix: Renamed backend parameter from status_filter to status to match
the frontend query string (?status=approved). The mismatch caused every
request to default to "pending" regardless of the tab selected.

Also improved the "pending" filter to exclude rejected reviews
(is_approved=False AND review_text != "__rejected__") instead of just
checking is_approved=False.

New:
- DELETE /api/v1/admin/reviews/{id} (hard delete, admin-only)
- deleteReview() method in frontend adminApi
- "Delete" button on every review card with AlertDialog confirmation

Co-authored-by: CommandCodeBot <noreply@commandcode.ai>
