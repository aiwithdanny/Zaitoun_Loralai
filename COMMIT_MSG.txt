Fix: category query param not being read on product detail page

wouter's useLocation() returns pathname only — the query string was
lost when parsing with location.split("?")[1]. Switched to using
window.location.search directly (the standard browser API), which
always returns the query string regardless of router implementation.

This fixes the ?category=Bottles/Cans/Bulk default-variant behavior.

Co-authored-by: CommandCodeBot <noreply@commandcode.ai>
