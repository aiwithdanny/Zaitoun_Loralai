Admin: Add product_group_id and size_label fields to ProductForm

New fields in the admin add/edit product modal:
- Group ID: slug-like identifier (e.g., "extra-virgin-olive-oil")
- Size Label: display text (e.g., "250ml", "3L", "5L")

Both optional. Grouped together in a "Product Group" section after
Category, with helper text explaining that products sharing a Group ID
appear as size variants on the product detail page.

If one field is set without the other, a subtle amber hint warns the
admin to consider setting both.

No backend changes needed — the schema and API already support these fields.

Co-authored-by: CommandCodeBot <noreply@commandcode.ai>
