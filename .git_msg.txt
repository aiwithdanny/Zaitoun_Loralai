Phase 2: Grouped product display with SizeSelector  
  
Adds ProductGroupCard and SizeSelector components. Products with  
the same product_group_id now render as one card with  
selectable size pills instead of individual cards.  
  
Files:  
- New: SizeSelector.tsx, ProductGroupCard.tsx  
- Updated: ProductGrid.tsx (grouping logic), api.ts (Product interface),  
  mockData.ts (new fields), useProducts.ts (filter type)  
  
Pre-group filtering: search/category filters still hit individual  
products; groups dynamically shrink to show only matching variants.  
  
Co-authored-by: CommandCodeBot <noreply@commandcode.ai> 
